import { cva } from "class-variance-authority";

const containerConfig = cva( 
  [
    "p-4",
    "w-full",
  ],
  {
    variants: {
    },
    defaultVariants: {
    },
  });

export default containerConfig;
